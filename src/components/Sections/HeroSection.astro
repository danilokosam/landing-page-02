<section class="hero-section relative isolate overflow-hidden rounded-2xl">
  <!-- Content -->
  <div
    class="relative flex min-h-[90vh] flex-col items-center justify-center  px-1.5 py-20 text-center sm:px-8 md:py-24"
  >
    <h1
      class="mx-auto max-w-4xl text-5xl font-bold leading-tight tracking-tight text-(--text) sm:text-5xl md:text-7xl"
    >
      Build a brand that
      <span class="hero-heading-accent">stands out online</span>
    </h1>

    <p
      class="mx-auto mt-5 max-w-xl text-base text-(--muted) px-4 sm:px-20 md:px-10 sm:mt-6 sm:max-w-2xl sm:text-lg md:text-xl"
    >
      We design websites, launch campaigns and craft experiences that turn
      visitors into loyal customers.
    </p>

    <div
      class="mt-8 flex w-full flex-col items-center justify-center gap-3 sm:mt-10 sm:w-auto sm:flex-row sm:gap-4"
    >
      <a
        class="w-full rounded-lg bg-(--primary) px-8 py-4 text-center font-semibold text-white shadow-lg shadow-(--primary)/25 transition-all duration-300 hover:-translate-y-1 hover:shadow-xl hover:shadow-(--primary)/30 sm:w-auto"
        href="#project-form"
      >
        Start Your Project
      </a>
      <!-- <a
        class="w-full rounded-lg border border-(--btn-secondary-border) px-8 py-4 text-center font-semibold text-(--btn-secondary-text) backdrop-blur-sm transition-all duration-300 hover:-translate-y-1 hover:bg-(--btn-secondary-hover) hover:text-white sm:w-auto"
        href="#"
      >
        View Our Work
      </a> -->
    </div>

    <!-- Stats -->
    <div
      class="mt-12 grid w-full max-w-3xl grid-cols-2 gap-4 sm:mt-16 md:grid-cols-4 md:gap-0 md:divide-x md:divide-(--card-border)"
    >
      <div class="flex flex-col items-center py-2">
        <span
          class="text-2xl font-bold text-(--primary) sm:text-3xl md:text-4xl"
          >120+</span
        >
        <span class="mt-1 text-xs text-(--muted) sm:text-sm"
          >Projects launched</span
        >
      </div>
      <div class="flex flex-col items-center py-2">
        <span
          class="text-2xl font-bold text-(--secondary) sm:text-3xl md:text-4xl"
          >97%</span
        >
        <span class="mt-1 text-xs text-(--muted) sm:text-sm"
          >Client retention</span
        >
      </div>
      <div class="flex flex-col items-center py-2">
        <span
          class="text-2xl font-bold text-(--accent-lime) sm:text-3xl md:text-4xl"
          >4.9</span
        >
        <span class="mt-1 text-xs text-(--muted) sm:text-sm">Avg. rating</span>
      </div>
      <div class="flex flex-col items-center py-2">
        <span class="text-2xl font-bold text-(--text) sm:text-3xl md:text-4xl"
          >24/7</span
        >
        <span class="mt-1 text-xs text-(--muted) sm:text-sm"
          >Dedicated support</span
        >
      </div>
    </div>
  </div>
</section>

<style>
  .hero-heading-accent {
    background: linear-gradient(135deg, var(--primary), var(--secondary));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }
</style>
